import {StyleSheet} from 'react-native';

const styles = colors =>
  StyleSheet.create({
    root: {
      flex: 1,
      padding: 20,
      justifyContent: 'center',
      backgroundColor: 'pink',
      alignItems: 'center',
    },
  });

export default styles;
